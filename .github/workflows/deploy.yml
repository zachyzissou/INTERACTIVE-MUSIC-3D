name: CI/CD - interactive-music-3d-unraid

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'docs/**'
      - 'README.md'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: [self-hosted, interactive-music-3d]
    timeout-minutes: 30

    env:
      NEXT_TELEMETRY_DISABLED: 1

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine changed paths
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            src:
              - '**/*'
              - '!docs/**'
              - '!README.md'

      - name: 🔄 Restore Next.js cache
        uses: actions/cache@v3
        with:
          path: .next/cache
          key: ${{ runner.os }}-nextjs-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-

      - name: 🔧 Set up Node.js
        if: steps.changes.outputs.src == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Cache npm
        if: steps.changes.outputs.src == 'true'
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: 📦 Install dependencies
        if: steps.changes.outputs.src == 'true'
        run: npm ci

      - name: 🔎 Type-check, lint & build
        if: steps.changes.outputs.src == 'true'
        run: npx tsc --noEmit && npm run lint && npm run build

      - name: 🛠 Set up Docker Buildx
        if: steps.changes.outputs.src == 'true'
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker-container

      - name: 🧹 Clean up existing containers and processes
        if: steps.changes.outputs.src == 'true'
        run: |
          echo "=== Conservative Cleanup Phase ==="
          
          # Only target our specific container
          echo "Stopping interactive-music-web container if running..."
          docker stop interactive-music-web 2>/dev/null || echo "Container not running"
          
          # Remove our specific container
          echo "Removing interactive-music-web container..."
          docker rm -f interactive-music-web 2>/dev/null || echo "Container not found"
          
          # Check if port 31415 is available
          echo "Checking port availability..."
          if netstat -tln 2>/dev/null | grep -q ':31415 '; then
            echo "⚠️  Port 31415 is occupied, using port 31416"
            echo "ALTERNATIVE_PORT=31416" >> $GITHUB_ENV
          else
            echo "✅ Port 31415 is available"
            echo "ALTERNATIVE_PORT=31415" >> $GITHUB_ENV
          fi
          
          echo "Cleanup completed successfully"

      - name: 🔨 Build Docker image
        if: steps.changes.outputs.src == 'true'
        env:
          DOCKER_BUILDKIT: 1
        run: |
          echo "=== Build Phase ==="
          echo "Building Docker image with buildx..."
          docker buildx build \
            --load \
            --cache-from=type=local,src=./.buildcache \
            --cache-to=type=local,dest=./.buildcache,mode=max \
            -t interactive-music-web . || {
              echo "❌ Docker build failed"
              exit 1
            }
          echo "✅ Docker build completed successfully"

      - name: 🚀 Start new container
        if: steps.changes.outputs.src == 'true'
        run: |
          echo "=== Deploy Phase ==="
          echo "Starting new container on port ${ALTERNATIVE_PORT}..."
          docker run -d --name interactive-music-web \
            --restart unless-stopped \
            -p ${ALTERNATIVE_PORT}:3000 \
            interactive-music-web || {
              echo "❌ Container start failed"
              docker logs interactive-music-web || true
              exit 1
            }
          echo "✅ Container started successfully on port ${ALTERNATIVE_PORT}"
          
          # Verify container is running
          sleep 3
          if docker ps | grep -q interactive-music-web; then
            echo "✅ Container is running and healthy"
            docker ps | grep interactive-music-web
            echo "🌐 Application should be accessible at: http://localhost:${ALTERNATIVE_PORT}"
          else
            echo "❌ Container failed to start properly"
            docker logs interactive-music-web || true
            exit 1
          fi
          
          # Test if the application is responding
          sleep 5
          if curl -f http://localhost:${ALTERNATIVE_PORT} >/dev/null 2>&1; then
            echo "✅ Application is responding on port ${ALTERNATIVE_PORT}"
          else
            echo "⚠️  Application may still be starting up on port ${ALTERNATIVE_PORT}"
          fi
